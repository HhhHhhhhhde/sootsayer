# SootSayer - 移动应用隐私泄漏检测平台 (后端)

SootSayer 是一个高性能、可扩展的移动应用隐私检测平台，专注于对 Android APK 进行深度的静态代码分析与隐私泄露风险判定。后端基于 Spring Boot 框架构建，采用了异步处理、策略模式及云原生组件，确保分析过程的高效与稳定。

## 🚀 核心特性

- **异步任务流水线**：采用后台异步处理机制，将高耗时的 APK 分析任务与 Web 请求解耦，确保系统极致响应。
- **静态分析引擎**：深度集成 **FlowDroid** (基于 Soot)，实现了精确的静态污点分析，能够精准提取潜在隐私泄露路径。
- **策略模式调度**：引入**策略模式**实现任务调度（`TaskScheduler`），支持即时测试、队列排队及优先级调度等多种模式切换。
- **云原生存储**：利用 **MinIO** 对象存储技术，提供高可靠的应用安装包（APK）及中间分析文件的存取支持。
- **分布式令牌管理**：集成 **Redis** 实现用户会话管理与安全令牌（Token）的集中化注销支持。
- **开发友好型架构**：
    - 统一的 RESTful API 响应格式 (`Result<T>`)。
    - 全局异常捕获机制 (`BusinessException`)。
    - 基于 ThreadLocal 的 `UserContext`，实现用户身份在各层间的透明传递。

## 🛠️ 技术栈

- **核心框架**：Java 17, Spring Boot 4.x
- **数据持久化**：MySQL, Spring Data JPA
- **缓存/会话**：Redis
- **对象存储**：MinIO
- **静态分析**：FlowDroid (Soot & InfoFlow)
- **其他工具**：Lombok, Maven, Jackson

## 📂 项目结构

```text
src/main/java/com/example/backend/
├── common/         # 公共工具类、统一响应封装、上下文映射
├── config/         # 系统配置类
│   ├── infra/      # 基础设施配置（Redis, 异步线程池, WebMvc）
│   ├── security/   # 安全配置（密码编码, 支付）
│   └── external/   # 外部集成配置
│       └── llm/    # LLM 配置及 Provider 实现
├── constants/      # 全局常量定义（任务状态、Redis 键名）
├── controller/     # REST 接口控制器层
├── dto/            # 数据传输对象 (DTO) 与 视图对象 (VO)
├── entity/         # JPA 数据库实体模型
├── exception/      # 自定义异常定义与全局异常处理器
├── interceptor/    # 拦截器（登录校验、权限控制）
├── payment/       # 支付策略模式实现
├── repository/     # Spring Data JPA 数据持久层接口
├── scheduler/      # 任务调度策略实现（策略模式核心）
├── security/       # 安全工具类（JWT）
├── service/        # 业务逻辑接口及其实现层
└── util/          # 通用工具类
```

## 🔧 LLM Provider 工厂模式

系统支持通过工厂模式灵活切换 LLM Provider：

- **rest-template**（默认）：调用真实的 LLM API
- **mock**：使用 Mock Provider，便于测试

配置方式（`application.yml`）：
```yaml
llm:
  api:
    provider-type: rest-template  # 或 mock
```

## 🤖 两轮 LLM 分析工作流

系统采用两轮 LLM 调用工作流，实现更深入的隐私泄露分析：

### 工作流架构

```
FlowDroid 分析结果
    ↓
┌─────────────────────────────────────────────────────────────┐
│  第一轮：并行逐条深度分析（qwen-plus + Thinking 模式）        │
│  - 对每个泄露路径并行调用                                    │
│  - 启用 enable_thinking 捕获深度推理过程                     │
│  - reasoning_content 字段存储思考过程                        │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│  第二轮：整体报告生成（qwen-max 最强模型）                    │
│  - 汇总所有分析结果                                          │
│  - 生成语义化整体摘要                                        │
└─────────────────────────────────────────────────────────────┘
    ↓
保存分析结果（含思考过程）
```

### 模型配置

| 用途 | 模型 | 说明 |
|------|------|------|
| 逐条深度分析 | `qwen-plus` | 支持 Thinking 模式，深度分析单个泄露路径 |
| 整体报告生成 | `qwen-max` | 最强模型，生成高质量语义摘要 |
| JSON 格式校验 | `qwen-flash` | 快速校验响应格式 |

### 抽象工厂模式

系统通过抽象工厂模式管理不同类型的 LLM Provider：

```java
// 工厂接口
public interface LlmModelFactory {
    LlmProvider createProvider();
    String getModelName();
    boolean supportsThinking();
}

// 三种工厂实现
ThinkingModelFactory    // qwen-plus - 深度思考
ReportModelFactory      // qwen-max - 整体报告
ValidationModelFactory  // qwen-flash - JSON校验
```

### 配置项（`.env`）

```bash
LLM_THINKING_MODEL=qwen-plus    # 第一轮：深度思考模型
LLM_REPORT_MODEL=qwen-max       # 第二轮：整体报告模型
LLM_VALIDATION_MODEL=qwen-flash  # JSON格式校验模型
```

### 提示模板

提示模板配置独立管理于 `application-llm-prompt.yml`：
- `llm.prompt.per-leak.*` - 逐条分析提示
- `llm.prompt.overall-report.*` - 整体报告提示

## ⚙️ 环境配置

项目使用 `.env` 文件管理所有敏感配置（如数据库密码、API Key、密钥等）。敏感信息不会提交到代码仓库。

### 1. 配置环境变量文件

从 `.env.example` 复制一份配置文件：

```bash
cp .env.example .env
```

然后编辑 `.env`，填入你的实际配置值。

### 2. 主要配置项

| 分类 | 变量名 | 说明 |
|------|--------|------|
| 数据库 | `MYSQL_HOST`, `MYSQL_PASSWORD`, `MYSQL_USERNAME` | MySQL 连接信息 |
| Redis | `SPRING_DATA_REDIS_HOST`, `SPRING_DATA_REDIS_PASSWORD` | Redis 连接信息 |
| 邮件 | `SPRING_MAIL_USERNAME`, `SPRING_MAIL_PASSWORD` | SMTP 发送邮箱配置 |
| JWT | `JWT_SECRET`, `JWT_EXPIRATION` | JWT 签名密钥和过期时间 |
| MinIO | `MINIO_ENDPOINT`, `MINIO_ACCESSKEY`, `MINIO_SECRETKEY` | 对象存储配置 |
| LLM | `LLM_API_KEY`, `LLM_API_BASE_URL`, `LLM_PROVIDER_TYPE`, `LLM_THINKING_MODEL`, `LLM_REPORT_MODEL`, `LLM_VALIDATION_MODEL` | LLM API 配置（provider-type: rest-template 或 mock）|
| 支付宝 | `ALIPAY_APP_ID`, `ALIPAY_PRIVATE_KEY`, `ALIPAY_PUBLIC_KEY`, `ALIPAY_GATEWAY` | 支付配置 |

### 3. application.yml

项目默认配置在 `src/main/resources/application.yml` 中，大部分配置项已通过环境变量引用，无需直接修改。

```yaml
spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/sootsayer
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:root}
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}
      password: ${SPRING_DATA_REDIS_PASSWORD:}

minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  accessKey: ${MINIO_ACCESSKEY:minioadmin}
  secretKey: ${MINIO_SECRETKEY:minioadmin}
  bucketName: ${MINIO_BUCKETNAME:sootsayer}

llm:
  api:
    base-url: ${LLM_API_BASE_URL:https://dashscope.aliyuncs.com/compatible-mode/v1}
    api-key: ${LLM_API_KEY}
    model: ${LLM_API_MODEL:qwen-flash}
    provider-type: ${LLM_PROVIDER_TYPE:rest-template}  # rest-template 或 mock
    thinking-model: ${LLM_THINKING_MODEL:qwen-plus}      # 第一轮深度思考模型
    report-model: ${LLM_REPORT_MODEL:qwen-max}          # 第二轮整体报告模型
    validation-model: ${LLM_VALIDATION_MODEL:qwen-flash} # JSON校验模型
```

> **注意**: `JWT_SECRET`、`LLM_API_KEY`、`MYSQL_PASSWORD` 等敏感值请务必通过 `.env` 文件配置，不要硬编码在代码中。

## 📦 快速上手

1. **克隆仓库**：
   ```bash
   git clone <repo-url>
   ```
2. **环境准备**：
   确保本地已安装 JDK 17 和 Maven 3.6+。
3. **编译项目**：
   ```bash
   mvn clean compile
   ```
4. **运行应用**：
   ```bash
   mvn spring-boot:run
   ```

## 📜 关键 API 概览

- `POST /api/auth/login`：用户登录并获取 Token。
- `POST /api/files/upload`：上传 APK 文件至云端存储。
- `POST /api/tasks`：提交新的分析检测任务。
- `GET /api/tasks/{id}/status`：实时查询任务进展与检测报告。

## 🐳 容器化部署

本项目支持使用 Docker 和 Docker Compose 进行一键化部署。

1. **环境依赖**：确保本地已安装 [Docker](https://www.docker.com/) 和 [Docker Compose](https://docs.docker.com/compose/)。
2. **配置环境变量**：确保 `.env` 文件已创建并包含所有必要的配置（参考上述环境配置）。
3. **一键启动**：
   ```bash
   docker-compose up -d --build
   ```
4. **服务访问**：
   - 后端 API：`http://localhost:8080`
   - MySQL：`localhost:3306`
   - Redis：`localhost:6379`
   - MinIO 控制台：`http://localhost:9001` (账号密码: `minioadmin` / `minioadmin`)

---
*本项目支持生产级别的容器化编排。*
