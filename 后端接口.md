# SootSayer 后端接口快速参考

## 接口总览

| 模块 | 接口数 | 主要功能 |
|------|--------|--------|
| 认证 (Auth) | 7 | 注册、登录、密码管理 |
| 文件 (File) | 1 | APK 文件上传 |
| 任务 (Task) | 6 | 提交、查询、管理分析任务 |
| 报告 (Report) | 3 | 获取分析报告、修复建议、导出 |
| 订阅 (Subscription) | 4 | 订阅管理 |
| 资产 (Asset) | 2 | 充值、交易记录 |
| 隐私 (Privacy) | 1 | 数据擦除 |
| 支付 (Payment) | 2 | 支付状态查询、回调处理 |

**总计：26 个接口**

---

## 快速命令参考

### 认证流程
```bash
# 1. 发送验证码
curl -X POST http://localhost:8080/api/auth/verify-code/send \
  -H "Content-Type: application/json" \
  -d '{"email":"user@example.com","type":"REGISTER"}'

# 2. 注册
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"user@example.com","password":"Pass123!","verifyCode":"123456"}'

# 3. 登录（获取 Token）
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"user@example.com","password":"Pass123!"}'

# 4. 获取用户信息
curl -X GET http://localhost:8080/api/auth/user/info \
  -H "Authorization: Bearer {TOKEN}"
```

### 任务流程
```bash
# 1. 提交分析任务
curl -X POST http://localhost:8080/api/tasks \
  -F "file=@app.apk" \
  -F "apkName=MyApp" \
  -H "Authorization: Bearer {TOKEN}"

# 2. 查询任务状态
curl -X GET http://localhost:8080/api/tasks/{taskId}/status \
  -H "Authorization: Bearer {TOKEN}"

# 3. 获取分析结果
curl -X GET http://localhost:8080/api/tasks/{taskId}/results \
  -H "Authorization: Bearer {TOKEN}"

# 4. 获取语义报告
curl -X GET http://localhost:8080/api/reports/{taskId}/semantic \
  -H "Authorization: Bearer {TOKEN}"

# 5. 导出报告
curl -X GET "http://localhost:8080/api/reports/{taskId}/export?format=pdf" \
  -H "Authorization: Bearer {TOKEN}" \
  -o report.pdf
```

---

## 接口详细列表

### 1️⃣ 认证接口 (Auth)

| 方法 | 端点 | 需登录 | 说明 |
|------|------|--------|------|
| POST | `/api/auth/verify-code/send` | ❌ | 发送验证码 |
| POST | `/api/auth/register` | ❌ | 用户注册 |
| POST | `/api/auth/login` | ❌ | 用户登录 |
| POST | `/api/auth/logout` | ✅ | 退出登录 |
| POST | `/api/auth/password/change` | ✅ | 修改密码 |
| POST | `/api/auth/password/reset` | ❌ | 重置密码 |
| GET | `/api/auth/user/info` | ✅ | 获取用户信息 |

### 2️⃣ 文件接口 (File)

| 方法 | 端点 | 需登录 | 说明 |
|------|------|--------|------|
| POST | `/api/files/upload` | ✅ | 上传文件 |

### 3️⃣ 任务接口 (Task)

| 方法 | 端点 | 需登录 | 说明 |
|------|------|--------|------|
| POST | `/api/tasks` | ✅ | 提交分析任务 |
| GET | `/api/tasks` | ✅ | 获取任务历史 |
| GET | `/api/tasks/{id}/status` | ✅ | 查询任务状态 |
| GET | `/api/tasks/{id}/results` | ✅ | 获取分析结果 |
| POST | `/api/tasks/{id}/cancel` | ✅ | 取消任务 |
| POST | `/api/tasks/{id}/retry` | ✅ | 重试任务 |

### 4️⃣ 报告接口 (Report)

| 方法 | 端点 | 需登录 | 说明 |
|------|------|--------|------|
| GET | `/api/reports/{taskId}/semantic` | ✅ | 获取语义分析报告 |
| GET | `/api/reports/{taskId}/fixes` | ✅ | 获取修复建议 |
| GET | `/api/reports/{taskId}/export` | ✅ | 导出报告 |

### 5️⃣ 订阅接口 (Subscription)

| 方法 | 端点 | 需登录 | 说明 |
|------|------|--------|------|
| POST | `/api/subscriptions` | ✅ | 订阅计划 |
| GET | `/api/subscriptions/current` | ✅ | 获取当前订阅 |
| POST | `/api/subscriptions/current/cancel` | ✅ | 取消订阅 |
| POST | `/api/subscriptions/current/renew` | ✅ | 续费订阅 |

### 6️⃣ 资产接口 (Asset)

| 方法 | 端点 | 需登录 | 说明 |
|------|------|--------|------|
| POST | `/api/assets/recharge` | ✅ | 充值 |
| GET | `/api/assets/transactions` | ✅ | 获取交易记录 |

### 7️⃣ 隐私接口 (Privacy)

| 方法 | 端点 | 需登录 | 说明 |
|------|------|--------|------|
| POST | `/api/privacy/data/erase` | ✅ | 数据擦除 |

### 8️⃣ 支付接口 (Payment)

| 方法 | 端点 | 需登录 | 说明 |
|------|------|--------|------|
| GET | `/api/payment/status/{outTradeNo}` | ✅ | 查询支付状态 |
| POST | `/api/payment/callback/{payMethod}` | ✅ | 支付回调 |

---

## 常用参数说明

### 验证码类型 (type)
- `REGISTER` - 注册验证码
- `RESET_PASSWORD` - 重置密码验证码

### 订阅计划 (planType)
- `FREE` - 免费版
- `BASIC` - 基础版
- `PREMIUM` - 高级版
- `ENTERPRISE` - 企业版

### 支付方式 (payMethod)
- `ALIPAY` - 支付宝
- `MOCK` - 模拟支付（测试用）

### 报告格式 (format)
- `pdf` - PDF 格式
- `html` - HTML 格式

### 任务状态 (status)
- `PENDING` - 待处理
- `PROCESSING` - 处理中
- `COMPLETED` - 已完成
- `FAILED` - 失败
- `CANCELLED` - 已取消

---

## 响应格式

所有接口返回统一的 JSON 格式：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

### 错误响应
```json
{
  "code": 400,
  "message": "请求参数错误",
  "data": null
}
```

### 常见错误码
| 错误码 | 含义 |
|--------|------|
| 0 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权/Token 过期 |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 500 | 服务器错误 |

---

## 认证方式

### Token 获取
1. 调用 `/api/auth/login` 获取 Token
2. Token 有效期：24 小时
3. 过期后需要重新登录

### Token 使用
在请求头中添加：
```
Authorization: Bearer {token}
```

### Token 注销
调用 `/api/auth/logout` 立即注销 Token

---

## 文件上传

### 支持的文件类型
- APK 文件（Android 应用包）

### 上传方式
使用 multipart/form-data：
```bash
curl -X POST http://localhost:8080/api/files/upload \
  -F "file=@/path/to/file.apk" \
  -H "Authorization: Bearer {token}"
```

### 文件大小限制
- 默认：5GB（根据 MinIO 配置）

---

## 分页参数

### 任务历史查询
```bash
curl -X GET "http://localhost:8080/api/tasks?page=0&size=10" \
  -H "Authorization: Bearer {token}"
```

- `page`: 页码（从 0 开始）
- `size`: 每页数量（默认 10）

---

## 常见使用场景

### 场景 1: 完整的 APK 分析流程
```bash
# 1. 登录
TOKEN=$(curl -s -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"user@example.com","password":"Pass123!"}' | jq -r '.data')

# 2. 提交任务
TASK_ID=$(curl -s -X POST http://localhost:8080/api/tasks \
  -F "file=@app.apk" \
  -H "Authorization: Bearer $TOKEN" | jq -r '.data')

# 3. 轮询查询状态（每 5 秒查询一次）
while true; do
  STATUS=$(curl -s -X GET http://localhost:8080/api/tasks/$TASK_ID/status \
    -H "Authorization: Bearer $TOKEN" | jq -r '.data.status')
  echo "Task Status: $STATUS"
  if [ "$STATUS" = "COMPLETED" ] || [ "$STATUS" = "FAILED" ]; then
    break
  fi
  sleep 5
done

# 4. 获取报告
curl -X GET http://localhost:8080/api/reports/$TASK_ID/semantic \
  -H "Authorization: Bearer $TOKEN" | jq .

# 5. 导出报告
curl -X GET "http://localhost:8080/api/reports/$TASK_ID/export?format=pdf" \
  -H "Authorization: Bearer $TOKEN" \
  -o report.pdf
```

### 场景 2: 批量查询任务
```bash
# 获取第一页的任务列表
curl -X GET "http://localhost:8080/api/tasks?page=0&size=20" \
  -H "Authorization: Bearer {token}" | jq '.data.content[] | {id, status, createdAt}'
```

### 场景 3: 订阅管理
```bash
# 订阅高级版
curl -X POST "http://localhost:8080/api/subscriptions?planType=PREMIUM" \
  -H "Authorization: Bearer {token}"

# 查看当前订阅
curl -X GET http://localhost:8080/api/subscriptions/current \
  -H "Authorization: Bearer {token}"

# 续费
curl -X POST http://localhost:8080/api/subscriptions/current/renew \
  -H "Authorization: Bearer {token}"
```

---

## 调试技巧

### 1. 保存完整响应
```bash
curl -v -X GET http://localhost:8080/api/auth/user/info \
  -H "Authorization: Bearer {token}" \
  -o response.json 2>&1
```

### 2. 格式化 JSON 输出
```bash
curl -s http://localhost:8080/api/auth/user/info \
  -H "Authorization: Bearer {token}" | jq .
```

### 3. 只显示响应头
```bash
curl -i -X GET http://localhost:8080/api/auth/user/info \
  -H "Authorization: Bearer {token}"
```

### 4. 显示请求和响应
```bash
curl -v -X GET http://localhost:8080/api/auth/user/info \
  -H "Authorization: Bearer {token}"
```

### 5. 测试连接
```bash
curl -I http://localhost:8080/api/auth/user/info
```

---

## 环境变量配置

### Linux/Mac
```bash
export BASE_URL="http://localhost:8080"
export TOKEN="your_token_here"

curl -X GET $BASE_URL/api/auth/user/info \
  -H "Authorization: Bearer $TOKEN"
```

### Windows (PowerShell)
```powershell
$BASE_URL = "http://localhost:8080"
$TOKEN = "your_token_here"

curl -X GET "$BASE_URL/api/auth/user/info" `
  -H "Authorization: Bearer $TOKEN"
```

---

## 相关文件

- **详细指南**：`CURL_TEST_GUIDE.md`
- **自动化测试脚本**：`test_api.sh` (Linux/Mac) 或 `test_api.bat` (Windows)
- **项目文档**：`README.md`

---

## 支持

如有问题，请查看：
1. 后端日志：`mvn spring-boot:run` 的输出
2. 数据库连接：确保 MySQL 已启动
3. 缓存服务：确保 Redis 已启动
4. 对象存储：确保 MinIO 已启动


