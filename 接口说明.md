# SootSayer API 接口文档

> SootSayer LLM-guided Android Privacy Leak Detection Cloud Platform

---

## 通用说明

### Base URL
```
http://{host}:{port}/api
```

### 认证方式
除登录注册接口外，所有接口均需在请求头中携带 JWT Token：
```
Authorization: Bearer <token>
```

### 通用响应格式
```json
{
  "code": 200,
  "msg": "success",
  "data": { ... }
}
```

| code | 说明 |
|------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权（Token 失效或缺失） |
| 500 | 服务器内部错误 |

---

## 1. 认证模块 `/api/auth`

### 1.1 发送验证码
```
POST /api/auth/verify-code/send
Content-Type: application/json
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| email | string | 是 | 邮箱地址 |
| type | string | 是 | 验证码类型：`REGISTER` / `RESET` |

**请求示例：**
```json
{
  "email": "user@example.com",
  "type": "REGISTER"
}
```

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

### 1.2 用户注册
```
POST /api/auth/register
Content-Type: application/json
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| username | string | 否 | 用户名（2-50字符） |
| password | string | 是 | 密码（8-20字符，需包含字母和数字） |
| email | string | 是 | 邮箱地址 |
| phone | string | 否 | 手机号（可选，1开头的11位数字） |
| verifyCode | string | 是 | 邮箱验证码 |

**请求示例：**
```json
{
  "username": "john_doe",
  "password": "Pass1234",
  "email": "user@example.com",
  "phone": "13800138000",
  "verifyCode": "123456"
}
```

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

### 1.3 用户登录
```
POST /api/auth/login
Content-Type: application/json
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| account | string | 是 | 账号（邮箱） |
| password | string | 是 | 密码 |

**请求示例：**
```json
{
  "account": "user@example.com",
  "password": "Pass1234"
}
```

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": "eyJhbGciOiJIUzI1NiJ9..."
}
```

---

### 1.4 退出登录
```
POST /api/auth/logout
Authorization: Bearer <token>
```

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

### 1.5 修改密码
```
POST /api/auth/password/change
Authorization: Bearer <token>
Content-Type: application/json
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| oldPassword | string | 是 | 旧密码 |
| newPassword | string | 是 | 新密码（8-20字符，需包含字母和数字） |

**请求示例：**
```json
{
  "oldPassword": "OldPass123",
  "newPassword": "NewPass456"
}
```

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

### 1.6 找回密码
```
POST /api/auth/password/reset
Content-Type: application/json
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| email | string | 是 | 邮箱地址 |
| verifyCode | string | 是 | 验证码 |
| newPassword | string | 是 | 新密码（8-20字符，需包含字母和数字） |

**请求示例：**
```json
{
  "email": "user@example.com",
  "verifyCode": "123456",
  "newPassword": "NewPass456"
}
```

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

### 1.7 获取当前用户信息
```
GET /api/auth/user/info
Authorization: Bearer <token>
```

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "id": 1,
    "username": "john_doe",
    "email": "user@example.com",
    "phone": "13800138000",
    "createTime": "2026-01-01T10:00:00"
  }
}
```

---

## 2. 文件模块 `/api/files`

### 2.1 上传文件
```
POST /api/files/upload
Content-Type: multipart/form-data
Authorization: Bearer <token>
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file | file | 是 | 待上传的文件（APK） |

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": "http://minio:9000/bucket/path/to/file.apk"
}
```

---

## 3. 任务模块 `/api/tasks`

### 3.1 提交检测任务
```
POST /api/tasks
Authorization: Bearer <token>
Content-Type: multipart/form-data
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file | file | 是 | APK 文件 |
| apkName | string | 否 | APK 名称 |
| apkSize | number | 否 | APK 大小（字节） |
| apkHash | string | 否 | APK SHA-256 哈希 |

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": 12345
}
```
> 返回值：任务 ID (taskId)

---

### 3.2 获取任务历史
```
GET /api/tasks
Authorization: Bearer <token>
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 | 默认值 |
|--------|------|------|------|--------|
| page | int | 否 | 页码（从0开始） | 0 |
| size | int | 否 | 每页条数 | 10 |

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "content": [
      {
        "id": 12345,
        "apkName": "example.apk",
        "status": "COMPLETED",
        "createTime": "2026-01-01T10:00:00"
      }
    ],
    "totalElements": 100,
    "totalPages": 10,
    "size": 10,
    "number": 0
  }
}
```

---

### 3.3 获取任务状态
```
GET /api/tasks/{id}/status
Authorization: Bearer <token>
```

**路径参数：**
| 参数名 | 类型 | 说明 |
|--------|------|------|
| id | long | 任务 ID |

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "id": 12345,
    "status": "SCANNING",
    "progress": 65,
    "currentStep": "FlowDroid Analysis",
    "createTime": "2026-01-01T10:00:00",
    "updateTime": "2026-01-01T10:05:00"
  }
}
```

**任务状态说明：**
| 状态 | 说明 |
|------|------|
| WAITING | 等待处理 |
| SCANNING | 扫描中 |
| AI_AUDITING | AI 审核中 |
| COMPLETED | 已完成 |
| FAILED | 失败 |
| CANCELLED | 已取消 |

---

### 3.4 获取任务分析结果
```
GET /api/tasks/{id}/results
Authorization: Bearer <token>
```

**路径参数：**
| 参数名 | 类型 | 说明 |
|--------|------|------|
| id | long | 任务 ID |

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": [
    {
      "source": "android.telephony.TelephonyManager.getDeviceId()",
      "sink": "android.util.Log.d()",
      "path": ["TelephonyManager", "StringBuilder", "Log"],
      "isTruePositive": true
    }
  ]
}
```

---

### 3.5 取消任务
```
POST /api/tasks/{id}/cancel
Authorization: Bearer <token>
```

**路径参数：**
| 参数名 | 类型 | 说明 |
|--------|------|------|
| id | long | 任务 ID |

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

### 3.6 重试任务
```
POST /api/tasks/{id}/retry
Authorization: Bearer <token>
```

**路径参数：**
| 参数名 | 类型 | 说明 |
|--------|------|------|
| id | long | 任务 ID |

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

## 4. 报告模块 `/api/reports`

### 4.1 获取语义分析报告
```
GET /api/reports/{taskId}/semantic
Authorization: Bearer <token>
```

**路径参数：**
| 参数名 | 类型 | 说明 |
|--------|------|------|
| taskId | long | 任务 ID |

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "reportId": 1,
    "taskId": 12345,
    "riskLevel": "HIGH",
    "overallScore": 75,
    "summary": "检测到5个高风险隐私泄露点...",
    "contentJson": "{...}",
    "createTime": "2026-01-01T10:30:00",
    "riskItems": [
      {
        "leakId": 1,
        "isTrueLeak": true,
        "riskLevel": "HIGH",
        "riskScore": 85,
        "analysis": "该应用获取了设备IMEI并通过Log输出...",
        "dataFlowExplanation": "TelephonyManager.getDeviceId() → String变量 → Log.d()",
        "legalImplications": "违反《个人信息保护法》第十三条...",
        "remediation": "建议使用 ANDROID_ID 替代 IMEI...",
        "complianceReferences": ["个人信息保护法第十三条", "GDPR第5条"]
      }
    ]
  }
}
```

---

### 4.2 获取修复建议
```
GET /api/reports/{taskId}/fixes
Authorization: Bearer <token>
```

**路径参数：**
| 参数名 | 类型 | 说明 |
|--------|------|------|
| taskId | long | 任务 ID |

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": [
    {
      "riskItemId": 1,
      "riskTitle": "设备ID泄露",
      "type": "DEVICE_ID",
      "suggestion": "使用 Android ID 替代 IMEI",
      "codeExample": "Settings.Secure.getString(context.getContentResolver(), Settings.Secure.ANDROID_ID)",
      "referenceLink": "https://developer.android.com/reference/android/provider/Settings.Secure#ANDROID_ID"
    }
  ]
}
```

---

### 4.3 导出报告
```
GET /api/reports/{taskId}/export
Authorization: Bearer <token>
```

**路径参数：**
| 参数名 | 类型 | 说明 |
|--------|------|------|
| taskId | long | 任务 ID |

**查询参数：**
| 参数名 | 类型 | 必填 | 说明 | 默认值 |
|--------|------|------|------|--------|
| format | string | 否 | 导出格式：`pdf` / `json` | pdf |

**响应：** 文件二进制流

---

## 5. 隐私数据模块 `/api/privacy`

### 5.1 注销账户（抹除数据）
```
POST /api/privacy/data/erase
Authorization: Bearer <token>
Content-Type: application/json
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| verifyPassword | string | 是 | 验证密码 |

**请求示例：**
```json
{
  "verifyPassword": "MyPassword123"
}
```

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

## 6. 资产模块 `/api/assets`

### 6.1 充值
```
POST /api/assets/recharge
Authorization: Bearer <token>
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| amount | decimal | 是 | 充值金额 |
| payMethod | string | 是 | 支付方式（如 `ALIPAY`, `WECHAT`） |

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

### 6.2 获取交易记录
```
GET /api/assets/transactions
Authorization: Bearer <token>
```

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": [
    {
      "id": 1,
      "userId": 1,
      "amount": 100.00,
      "type": "RECHARGE",
      "description": "充值",
      "method": "ALIPAY",
      "status": "SUCCESS",
      "createTime": "2026-01-01T10:00:00"
    }
  ]
}
```

**交易类型说明：**
| type | 说明 |
|------|------|
| RECHARGE | 充值 |
| CONSUME | 消费 |
| REFUND | 退款 |

**交易状态说明：**
| status | 说明 |
|--------|------|
| SUCCESS | 成功 |
| PENDING | 处理中 |
| FAILED | 失败 |

---

## 7. 订阅模块 `/api/subscriptions`

### 7.1 订阅套餐
```
POST /api/subscriptions
Authorization: Bearer <token>
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| planType | string | 是 | 套餐类型：`MONTHLY` / `YEARLY` |

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

### 7.2 获取当前订阅
```
GET /api/subscriptions/current
Authorization: Bearer <token>
```

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "id": 1,
    "userId": 1,
    "planType": "MONTHLY",
    "startTime": "2026-01-01T00:00:00",
    "endTime": "2026-02-01T00:00:00",
    "status": 1,
    "createTime": "2026-01-01T00:00:00",
    "updateTime": "2026-01-01T00:00:00"
  }
}
```

**状态说明：**
| status | 说明 |
|--------|------|
| 1 | 有效 |
| 0 | 已过期/已取消 |

---

### 7.3 取消订阅
```
POST /api/subscriptions/current/cancel
Authorization: Bearer <token>
```

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

### 7.4 续订订阅
```
POST /api/subscriptions/current/renew
Authorization: Bearer <token>
```

**响应示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

## 附录

### A. 错误码说明

| 错误码 | HTTP状态码 | 说明 |
|--------|------------|------|
| 200 | 200 | 成功 |
| 400 | 400 | 请求参数错误（字段校验失败） |
| 401 | 401 | 未授权（Token 无效或已过期） |
| 403 | 403 | 禁止访问（无权限） |
| 404 | 404 | 资源不存在 |
| 500 | 500 | 服务器内部错误 |

### B. 风险等级说明

| 等级 | 说明 |
|------|------|
| LOW | 低风险 |
| MEDIUM | 中风险 |
| HIGH | 高风险 |

### C. LLM 分析结果字段说明 (`LlmAnalysisResult`)

| 字段 | 类型 | 说明 |
|------|------|------|
| leakId | long | 对应的泄露记录 ID |
| isTrueLeak | boolean | 是否为真实隐私泄露 |
| falsePositiveReason | string | 被判定为误报的原因 |
| riskLevel | string | 风险等级（HIGH/MEDIUM/LOW） |
| riskScore | int | 风险评分（0-100） |
| analysis | string | 自然语言分析结果（中文） |
| dataFlowExplanation | string | 数据从 Source 到 Sink 的流向说明 |
| legalImplications | string | 法律依据（中国《个人信息保护法》及 GDPR） |
| remediation | string | 修复建议 |
| complianceReferences | list | 合规参考（如 `["个人信息保护法第十三条", "GDPR第5条"]`） |

### D. 修复建议字段说明 (`FixSuggestionVO`)

| 字段 | 类型 | 说明 |
|------|------|------|
| riskItemId | long | 风险项 ID |
| riskTitle | string | 风险标题 |
| type | string | 风险类型 |
| suggestion | string | 修复建议 |
| codeExample | string | 代码示例 |
| referenceLink | string | 参考链接 |
